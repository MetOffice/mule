[project]
name = "mule"
version = "2025.10.1"
description = "API for interacting with UM output files"
readme = "README.md"
requires-python = ">=3.12.9"
dependencies = [
    "numpy == 2.2.6",
    "setuptools",
    "cpplint == 2.0.2",
    "ruff==0.12.1",
    "shellcheck-py==0.10.0.1",
]
